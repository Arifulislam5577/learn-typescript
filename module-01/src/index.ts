type AddNumber = (n1: number, n2: number) => number;

const addNumber: AddNumber = (n1, n2) => n1 + n2;

const addNumber1 = (n1: number, n2: number): number => n1 + n2;

function addNumber2(n1: number, n2: number): number {
  return n1 + n2;
}
